# Personal brand lane (fork)

Isolated from `memory/grok/`. Use `pb-*` scripts only — never edit grok files.

| Component | Status |
|-----------|--------|
| `pb-pipeline.js` | stub — fork from grok-pipeline |
| `carousel-gates.js` | stub |
| `session_state.md` | separate tracker |
| Tabs | 3 default (Leah, Catalina, Isabella) |

Send requires `PB_GO_SEND_CAROUSEL=1`.
